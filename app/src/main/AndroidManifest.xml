<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.alfred.chowder">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ui.AlfredApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.alfred.chowder.ui.activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity android:name=".ui.activity.CustomActivity">

        </activity>
        <activity android:name=".ui.activity.InputLimitActivity">

        </activity>

        <activity
            android:name=".ui.activity.QQHealthViewActivity"
            android:label="@string/activity_title_qq_health">
        </activity>

        <activity
            android:name=".ui.activity.RxJavaAndRetrofitActivity"
            android:label="@string/activity_title_rx_retrofit">

        </activity>

        <activity android:name=".ui.activity.AnimationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

        <activity android:name=".ui.activity.RecyclerViewActivity">

        </activity>
    </application>

</manifest>